# TASK-0006｜验证并回写

## 对应需求
- `REQ-0002` 同一机会包展示机会验真结果与 DCF 估值联动

## 验证口径
- 正文验证：`docs/top20_opportunity_pack_real_3markets.md` 必须同时出现 `机会验真`、`估值联动`、`风险提示`
- 数据验证：`output/top20_first_batch_opportunities_real_3markets.csv` 必须出现 `validation_summary`、`valuation_summary`、`risk_summary`
- 行为验证：三市场主编排应在机会验真产物生成后，重新生成最终机会包
- 回写验证：`backlog.json`、`kpi_snapshot.json`、项目管理主看板必须同步更新

## 验证证据
- 正文证据：`docs/top20_opportunity_pack_real_3markets.md` 中首批机会池表头与样例行已包含三类摘要
- 数据证据：`output/top20_first_batch_opportunities_real_3markets.csv` 已导出三类摘要字段
- 任务证据：`tests.test_top20_pack` 与 `tests.test_generate_top20_opportunity_pack` 共 9 项通过
- 系统证据：`scripts/run_real_pack_3markets.sh` 已串上 `--validation-positions-json`

## 回写结果
- `TASK-0005` 已完成并记录实现摘要
- 当前将 `TASK-0006` 作为验证/回写闭环完成
- `ITER-0002` 在本次回写后应进入 `done`
- 下一步切换到 `REQ-0003`，开始字段级来源映射与可信度评分

## 建议下一步
- 优先推进 `TASK-0007`：明确字段级来源映射的边界、展示层级与可信度评分规则
