# TASK-0009｜验证并回写

## 对应需求
- `REQ-0003` 机会包补齐字段级来源映射与可信度评分

## 验证口径
- 代码层：新增 trust 输入与 pack 渲染字段后，相关 Python 模块可通过编译
- 测试层：机会包渲染、pack 注入、trust_chain 三组聚焦测试全部通过
- 产物层：真实机会包正文已出现 `可信度`、`来源摘要`；CSV 已出现 trust / lineage 相关字段
- 编排层：三市场主脚本语法正确，且顺序为 validation -> trust_chain -> final pack refresh

## 验证结果
- `python3 -m py_compile src/investor_method_lab/top20_pack.py scripts/generate_top20_opportunity_pack.py scripts/build_opportunity_trust_chain.py` 通过
- `python3 -m unittest tests.test_top20_pack tests.test_generate_top20_opportunity_pack tests.test_opportunity_trust_chain -v` 共 12 项通过
- `bash -n scripts/run_real_pack_3markets.sh` 通过
- 真实产物验证：
  - `docs/top20_opportunity_pack_real_3markets.md` 已显示 `可信度`、`来源摘要`
  - `output/top20_first_batch_opportunities_real_3markets.csv` 已包含 `confidence_summary`、`source_lineage_summary`

## 回写结果
- `TASK-0008` 完成并已记录实现产物
- `TASK-0009` 作为验证/回写闭环完成
- `ITER-0003` 完成后，`REQ-0003` 当前三任务闭环完成

## 建议下一步
- 观察一轮真实日更产物，确认 `可信度 / 来源摘要` 是否足够可读
- 若需要继续建设，再提出新需求：例如把 review queue 接入 Feishu/看板，或把来源摘要做成可点开的附录链接
