# TASK-0005｜完成核心实现

## 对应需求
- `REQ-0002` 同一机会包展示机会验真结果与 DCF 估值联动

## 本次实现
- 在 `src/investor_method_lab/top20_pack.py` 新增统一摘要构造：`build_validation_summary`、`build_valuation_summary`、`build_risk_summary`
- 把机会包正文三张核心表统一升级为：`机会验真 / 估值联动 / 风险提示 / 原始理由 / 原始备注`
- 在 `scripts/generate_top20_opportunity_pack.py` 增加 `--validation-positions-json`，从 `output/opportunity_validation_positions_latest.json` 注入验真结果
- 同步扩展 CSV 输出字段，保留 `validation_summary`、`valuation_summary`、`risk_summary` 供后续系统联动
- 在 `scripts/run_real_pack_3markets.sh` 中追加“验真产物生成后重刷最终机会包”步骤

## 关键结果
- 同一个标的现在可在一行内同时看到：机会状态 / 超额收益 / 持有天数 / 估值来源与关键值 / 风险提示
- 报告正文复用现有 `docs/top20_opportunity_pack_real_3markets.md`
- 数据联动复用现有 `output/opportunity_validation_positions_latest.json`

## 主要产物
- `src/investor_method_lab/top20_pack.py`
- `scripts/generate_top20_opportunity_pack.py`
- `scripts/run_real_pack_3markets.sh`
- `docs/top20_opportunity_pack_real_3markets.md`
- `output/top20_first_batch_opportunities_real_3markets.csv`

## 验证
- `python3 -m py_compile src/investor_method_lab/top20_pack.py scripts/generate_top20_opportunity_pack.py`
- `python3 -m unittest tests.test_top20_pack tests.test_generate_top20_opportunity_pack -v`
- 真实产物抽查通过：正文与 CSV 已出现 `validation_summary`、`valuation_summary`、`risk_summary`
